road map
x create card.h to hold card struct and do card operations
x add card rendering function to card.h
x size cards a bit better
x render 6 card hand
x respond to mouse events
x start game button (menu)
x transition to and from playing state
x load player and opponent hands
x select card to play it (opponent always plays a random card at the same time)
x show played card in the middle, remove it from hand
x allow playing all cards in hand, removing cards in middle and
x return to menu after all cards are played
x bake attack and health on cards
x create pool of cards that can be used for starting hands
x implement battle turns, updating card appearence
x organize codebase
x implement some basic abilities
X color card stats relative to base state
x load shaders from file
x outline selectable cards
- make Strike ability selectable between opp hand and in play card
- add ability where if card is last card in hand it's destroyed
- refactor game sequence to allow co routining actions and triggered things


card property ideas
- levels?
- colors?
- rarities?
- cards that have abilities based on number of cards in either players hand (starting or dynamic)
- cards that impact cards in either players hand

game modes
- constraints based on properties (ex. only lvl 1 cards, or max 2 of each lvl, color restrictions, etc.)

turn sequence
- is game over? no cards in hand or in play for a player, end game if so
-- is player in play card empty? player selects card
--- animate card play
-- is opponent in pkay card empty? opponent selects card
--- animate card play
-- if player played card, does player in play card have on play abilities? if so, select target
-- if opponent played card, does opponent in play card have on play abilities? if so, opponent selects target
--- animate player target and effect
--- animate opponent target and effect
- resolve damamge - start over at the top
- start battle phase
- perform / animate haste attacks
- resolve damage - start over at top
- perform / animate regular attacks
- resolve damage - start over at top

